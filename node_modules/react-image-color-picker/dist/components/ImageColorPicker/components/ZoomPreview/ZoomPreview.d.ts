import React from 'react';
import { Coordinates } from '@/src/util';
type ColorPreviewProps = {
    coordinates: Coordinates;
    color: string;
    dimensions: {
        width: number;
        height: number;
    };
    image: string;
    zoom?: number;
};
export declare const ZoomPreview: ({ color, coordinates, dimensions, image, zoom }: ColorPreviewProps) => React.JSX.Element;
export {};
