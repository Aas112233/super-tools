export type Coordinates = {
    x: number;
    y: number;
};
export declare class Canvas {
    private readonly canvas;
    private readonly context;
    constructor(canvasElement: HTMLCanvasElement);
    listenMovements(listener: any): void;
    cleanUp(listener: any): void;
    drawImage(img: any): void;
    setDimensions(width: number, height: number): void;
    getDimensions(): {
        width: number;
        height: number;
    };
    getCanvasCenterPoint(): {
        x: number;
        y: number;
    };
    getCanvasCoordinates(coordinates: Coordinates): {
        x: number;
        y: number;
    };
    getPixelColor(coordinates: Coordinates): string;
}
